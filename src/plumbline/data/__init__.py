"""Packaged static data tables for data-driven rules (ADR-0017).

These are versioned, in-repo Python literals — never fetched at runtime — so the
detection path stays deterministic and network-free (CLAUDE.md §1.1).
"""
